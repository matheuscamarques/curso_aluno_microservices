import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.backoffice.webengenharia.dev',
  appName: 'backoffice',
  webDir: 'dist/backoffice',
  bundledWebRuntime: false
};

export default config;
